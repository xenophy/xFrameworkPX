* SSL接続判定

ここでは、SSL接続判定について学びます。

このチュートリアルでは、次の操作を行います。

+ SSL接続判定のコードを記述する

このコードは、xFrameworkPXを読み込むことで、どこでも利用することができます。

* SSL接続判定のコードを記述する

{{{
if (is_secure()) {
    // SSLでアクセスされた場合
}
}}}

SSLでアクセスされた場合に、trueを返します。